module.exports = {
  content: ["./src/**/*.{js,jsx,ts,tsx}"],
  theme: {
    extend: {
      colors: {
        darkBlack: "#0a0a0a", // A very dark black for the background
        jetBlack: "#111111", // Slightly lighter black
        lightGray: "#f3f4f6", // Light gray for text
        darkGray: "#4b5563",
      },
    },
  },
  plugins: [],
};
